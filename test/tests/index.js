'use strict';

module.exports = {
    connection: require('./connection'),
    agent: require('./agent'),
    queues: require('./queues'),
    device: require('./device'),
    call: require('./call')
};
